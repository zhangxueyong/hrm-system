<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zxy.hrm.audit.dao.UserDao">
    <select id="findUserByBame" resultType="com.zxy.hrm.audit.entity.SysUser">
        SELECT * from sys_user where user_number = #{username}
    </select>
</mapper>